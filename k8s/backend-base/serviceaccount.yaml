apiVersion: v1
kind: ServiceAccount
metadata:
  name: REPLACED-IN-OVERLAY
  # No annotations: EKS Pod Identity does NOT use IRSA annotations.
  # The Pod Identity association (namespace, SA name) is provisioned in Terraform.
  # The SA name here must exactly match the association created by Terraform.
