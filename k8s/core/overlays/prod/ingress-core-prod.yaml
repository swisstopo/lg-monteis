# SCAFFOLD — un-gated; prod AppProject does not yet exist.
# TODO: replace host with the production hostname when the environment is provisioned.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: core
  namespace: monteis-prod
spec:
  ingressClassName: traefik
  # No spec.tls block — TLS terminates at CloudFront (MANDATED).
  rules:
    - host: TODO-prod.swisstopo-monteis-test.swisstopo.cloud
      http:
        paths:
          - pathType: Prefix
            path: /api
            backend:
              service:
                name: core
                port:
                  name: http
