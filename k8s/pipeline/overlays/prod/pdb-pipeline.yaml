# SCAFFOLD — un-gated; prod AppProject does not yet exist.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: pipeline
  namespace: monteis-prod
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: pipeline
