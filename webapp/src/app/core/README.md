# Purpose

This folder contains everything that is a singleton service, such as interceptors, auth-guards, state-management, etc.

## Example
```
core/
      models/           <-- Contains models which are used throught the app or via API
      i18n/             <-- e.g. Language detection
```
