// Swapped for the e2e-only env-url.e2e.ts via angular.json's `e2e` build configuration
// (fileReplacements), so the same source only differs in which static, checked-in JSON it points
export const ENV_JSON_URL = '/env.json';
