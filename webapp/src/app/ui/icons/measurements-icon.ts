import { Component } from '@angular/core';

/**
 * Icon "text-link--analysis" from the IBM Carbon Design System (Apache-2.0),
 * https://github.com/carbon-design-system/carbon/tree/main/packages/icons
 *
 * Sized in `em` and filled with `currentColor` so it adopts the size and color of its host, e.g.
 * the workbench activity item.
 */
@Component({
  selector: 'app-measurements-icon',
  templateUrl: './measurements-icon.html',
})
export class MeasurementsIcon {}
